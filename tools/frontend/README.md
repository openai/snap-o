# Shared frontend styles

Network and Tweaks import `shared.css` directly. Vite bundles it into each tool’s frontend; it adds no runtime dependency.

Run either frontend’s formatting and lint checks to validate this directory. Tool-specific styles stay with their tool.
