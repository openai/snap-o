<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name=".ExampleInitProvider"
            android:authorities="${applicationId}.snapo.example"
            android:exported="false" />
    </application>
</manifest>
